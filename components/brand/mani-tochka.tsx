export function ManiTochka({ className = "" }: { className?: string }) {
  return <span className={`font-semibold text-primary ${className}`.trim()}>мани.точка</span>
}

export function ManiTochkaTitle({ className = "" }: { className?: string }) {
  return <span className={`font-semibold text-primary ${className}`.trim()}>Мани.точка</span>
}
